generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Transaction {
  id            String  @id @default(uuid())
  value         Float
  payment_type  String 
  category      String
  date          DateTime

  @@map("transactions")
}

